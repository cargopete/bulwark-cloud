# Lambda bundle requirements — excludes bulwark-cloud-shared (provided by layer).
# Keep in sync with api/pyproject.toml dependencies.
fastapi>=0.111
mangum>=0.17
pydantic>=2.7
pydantic-settings>=2.2
boto3>=1.34
python-ulid>=2.2
